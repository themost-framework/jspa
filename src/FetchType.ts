enum FetchType {
    /**
     * Defines that data must be eagerly fetched.
     */
    Eager = 'EAGER',
    /**
     * Defines that data can be lazily fetched.
     */
    Lazy = 'LAZY'
}

export {
    FetchType
}